# https://mise.jdx.dev/configuration.html
# Per-project overrides: create .mise.toml in any project directory

[tools]
# Pin Python to 3.13 so local + CI agree (brew also keeps python@3.13 since
# thefuck/liblqr depend on it, but mise is the userland default).
node = "lts"
python = "3.13"
go = "latest"
ruby = "latest"

[settings]
# Also reads .nvmrc, .python-version, .tool-versions
idiomatic_version_file_enable_tools = ["node", "python", "ruby", "go"]
not_found_auto_install = true
jobs = 4
