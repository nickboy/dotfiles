# Keeps the SHA-pinned GitHub Actions in ci.yml current: Dependabot opens
# a PR when an action publishes a new release, CI validates it, you merge.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
